<!DOCTYPE html>
<html>
<head>
    <title>SpinCycle Laundry - Home</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap');
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: #1a202c;
            line-height: 1.6;
            overflow-x: hidden;
        }
        .navbar {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.2);
            color: #1a202c;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 16px 32px;
            position: sticky;
            top: 0;
            z-index: 1000;
            transition: all 0.3s ease;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
        }
        .navbar.scrolled {
            background: rgba(255, 255, 255, 0.98);
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.15);
        }
        .navbar .brand {
            display: flex;
            align-items: center;
            font-size: 1.8em;
            font-weight: 700;
            color: #667eea;
            letter-spacing: -0.5px;
        }
        .navbar .logo-img {
            height: 42px;
            margin-right: 12px;
            border-radius: 12px;
            object-fit: contain;
            filter: drop-shadow(0 4px 8px rgba(102, 126, 234, 0.3));
        }
        .navbar ul {
            list-style: none;
            display: flex;
            gap: 32px;
            margin: 0;
            padding: 0;
        }
        .navbar ul li a {
            color: #4a5568;
            text-decoration: none;
            font-size: 1em;
            font-weight: 500;
            padding: 12px 20px;
            border-radius: 12px;
            position: relative;
            transition: all 0.3s ease;
        }
        .navbar ul li a:hover {
            color: #667eea;
            background: rgba(102, 126, 234, 0.1);
            transform: translateY(-2px);
        }
        .navbar .auth {
            display: flex;
            gap: 16px;
        }
        .navbar .auth a {
            text-decoration: none;
            font-weight: 600;
            font-size: 0.95em;
            border-radius: 25px;
            padding: 10px 24px;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }
        .navbar .auth a:first-child {
            color: #667eea;
            background: transparent;
            border: 2px solid #667eea;
        }
        .navbar .auth a:first-child:hover {
            background: #667eea;
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.3);
        }
        .navbar .auth a:last-child {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
        }
        .navbar .auth a:last-child:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.4);
        }
        .main-content {
            min-height: 100vh;
            background: white;
            border-radius: 32px 32px 0 0;
            margin-top: 0;
            position: relative;
            overflow: hidden;
        }
        .hero {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 120px 40px 80px;
            text-align: center;
            position: relative;
            overflow: hidden;
        }
        .hero-content {
            max-width: 800px;
            margin: 0 auto;
            position: relative;
            z-index: 2;
        }
        .hero h1 {
            font-size: 3.5em;
            margin-bottom: 24px;
            font-weight: 700;
            letter-spacing: -2px;
            line-height: 1.1;
            text-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            animation: slideInUp 0.8s ease-out;
        }
        .hero p {
            font-size: 1.3em;
            opacity: 0.9;
            font-weight: 400;
            animation: slideInUp 0.8s ease-out 0.2s both;
        }
        @keyframes slideInUp {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .section-title {
            text-align: center;
            padding: 80px 40px 60px;
            background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
        }
        .section-title h2 {
            font-size: 2.8em;
            font-weight: 700;
            color: #1a202c;
            margin-bottom: 16px;
            letter-spacing: -1px;
        }
        .section-title p {
            font-size: 1.2em;
            color: #64748b;
            max-width: 600px;
            margin: 0 auto;
        }
        .products {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 32px;
            margin: -40px 40px 80px;
            position: relative;
            z-index: 10;
        }
        .product-card {
            background: white;
            border-radius: 24px;
            padding: 40px 28px 32px;
            text-align: center;
            position: relative;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            border: 1px solid rgba(226, 232, 240, 0.8);
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }
        .product-card:hover {
            transform: translateY(-12px);
            box-shadow: 0 20px 60px rgba(102, 126, 234, 0.2);
        }
        .product-card .icon-wrapper {
            width: 100px;
            height: 100px;
            margin: 0 auto 24px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            transition: all 0.3s ease;
        }
        .product-card:hover .icon-wrapper {
            transform: scale(1.1) rotateY(10deg);
        }
        .product-card img {
            width: 50px;
            height: 50px;
            object-fit: contain;
            filter: brightness(0) invert(1);
        }
        .product-card h3 {
            font-size: 1.4em;
            margin-bottom: 16px;
            color: #1a202c;
            font-weight: 600;
            letter-spacing: -0.5px;
        }
        .product-card p {
            font-size: 1em;
            color: #64748b;
            margin-bottom: 24px;
            line-height: 1.6;
        }
        .product-card .price {
            font-weight: 700;
            font-size: 1.3em;
            color: #667eea;
            margin-bottom: 24px;
        }
        .product-card button {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            padding: 14px 32px;
            border-radius: 50px;
            cursor: pointer;
            font-size: 1em;
            font-weight: 600;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }
        .product-card button:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.4);
        }
        .product-card button:active {
            transform: translateY(0);
        }
        footer {
            background: linear-gradient(135deg, #1a202c 0%, #2d3748 100%);
            color: #cbd5e0;
            text-align: center;
            padding: 60px 40px 40px;
            position: relative;
        }
        footer p {
            font-size: 1.1em;
            margin-bottom: 24px;
            font-weight: 500;
        }
        footer a {
            color: #a0aec0;
            margin: 0 16px;
            text-decoration: none;
            font-size: 1em;
            transition: color 0.3s ease;
            position: relative;
        }
        footer a:hover {
            color: #667eea;
        }
        @media (max-width: 1024px) {
            .products {
                grid-template-columns: repeat(2, 1fr);
                margin: -40px 20px 60px;
                gap: 24px;
            }
        }
        @media (max-width: 768px) {
            .navbar ul { display: none; }
            .hero { padding: 80px 20px 60px; }
            .hero h1 { font-size: 2.5em; }
            .hero p { font-size: 1.1em; }
            .section-title { padding: 60px 20px 40px; }
            .section-title h2 { font-size: 2.2em; }
            .products { grid-template-columns: 1fr; margin: -40px 20px 60px; }
            .navbar { padding: 16px 20px; }
            .navbar .auth { display: none; }
        }
        @media (max-width: 480px) {
            .navbar .brand { font-size: 1.4em; }
            .navbar .auth { flex-direction: row; gap: 8px; }
            .navbar .auth a { padding: 8px 16px; font-size: 0.9em; }
            .hero h1 { font-size: 2em; }
            .product-card { padding: 32px 20px 24px; }
        }
        .animate-on-scroll {
            opacity: 0;
            transform: translateY(30px);
            transition: all 0.6s ease;
        }
        .animate-on-scroll.animated {
            opacity: 1;
            transform: translateY(0);
        }
    </style>
</head>
<body>
    <!-- Navigation Bar -->
    <nav class="navbar" id="navbar">
        <span class="brand">
            <img src="/images/laundry4.png" alt="SpinCycle Logo" class="logo-img">
            SpinCycle Laundry
        </span>
        <ul>
            <li><a href="/">Home</a></li>
            <li><a href="#services">Services</a></li>
            <li><a href="#about">About</a></li>
            <li><a href="#contact">Contact</a></li>
        </ul>
        <div class="auth">
            <a href="/users/login">Login</a>
            <a href="/users/register">Register</a>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="main-content">
        <section class="hero">
            <div class="hero-content">
                <h1>Premium Laundry Care</h1>
                <p>Experience the future of laundry services with SpinCycle - where convenience meets quality</p>
            </div>
        </section>

        <section class="section-title">
            <h2>Our Services</h2>
            <p>Professional laundry solutions designed to fit your busy lifestyle</p>
        </section>

        <section class="products" id="services">
            <div class="product-card animate-on-scroll">
                <div class="icon-wrapper">
                    <img src="https://img.icons8.com/color/96/000000/washing-machine.png" alt="Wash & Fold">
                </div>
                <h3>Wash & Fold</h3>
                <p>Drop off your laundry and pick it up clean, folded, and ready to go. Perfect for busy professionals.</p>
                <div class="price">₱120 per load</div>
                <button>Book Service</button>
            </div>

            <div class="product-card animate-on-scroll">
                <div class="icon-wrapper">
                    <img src="https://img.icons8.com/color/96/000000/iron.png" alt="Ironing Service">
                </div>
                <h3>Premium Ironing</h3>
                <p>Wrinkle-free clothes with our professional ironing service. Your garments will look crisp and fresh.</p>
                <div class="price">₱25 per piece</div>
                <button>Book Now</button>
            </div>

            <div class="product-card animate-on-scroll">
                <div class="icon-wrapper">
                    <img src="https://img.icons8.com/color/96/000000/clothes.png" alt="Dry Cleaning">
                </div>
                <h3>Dry Cleaning</h3>
                <p>Gentle care for your delicate and special garments. Trust us with your finest clothes.</p>
                <div class="price">₱180 per item</div>
                <button>Book Service</button>
            </div>

            <div class="product-card animate-on-scroll">
                <div class="icon-wrapper">
                    <img src="https://img.icons8.com/color/96/000000/delivery.png" alt="Pickup & Delivery">
                </div>
                <h3>Pickup & Delivery</h3>
                <p>Convenient laundry service right at your doorstep. We handle everything so you don't have to.</p>
                <div class="price">₱50 per trip</div>
                <button>Schedule Pickup</button>
            </div>
        </section>

        <footer>
            <p>&copy; 2025 SpinCycle Laundry. Making laundry effortless.</p>
            <div>
                <a href="#terms">Terms & Conditions</a>
                <a href="#privacy">Privacy Policy</a>
            </div>
        </footer>
    </div>

    <script>
        window.addEventListener('scroll', function() {
            const navbar = document.getElementById('navbar');
            if (window.scrollY > 100) {
                navbar.classList.add('scrolled');
            } else {
                navbar.classList.remove('scrolled');
            }
        });

        const observerOptions = { threshold: 0.1, rootMargin: '0px 0px -50px 0px' };

        const observer = new IntersectionObserver(function(entries) {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('animated');
                }
            });
        }, observerOptions);

        document.querySelectorAll('.animate-on-scroll').forEach(el => {
            observer.observe(el);
        });

        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({ behavior: 'smooth', block: 'start' });
                }
            });
        });

        setTimeout(() => {
            document.querySelectorAll('.product-card').forEach((card, index) => {
                card.style.animationDelay = `${index * 0.1}s`;
            });
        }, 100);
    </script>
</body>
</html>